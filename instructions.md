Create a container that can run comfui and can be deployed in the google cloud
- Create a docker image that installs confyui
- download the required models and install them in thr ight folders
- container needs to be deployed in the gogle cloud and its endpoints need to be accessible to other containers running in the same network in the same accoount

Base container can be found at this github - https://github.com/Comfy-Org/comfyui-ci-container.git


